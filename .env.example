# Development Environment Variables
# Copy this file to .env and update with your values

# Tailscale Configuration (for dev.docker-compose.yml)
# Used to map internal services (database, redis, minio) via Tailscale
TAILSCALE_HOST=snickers
TAILSCALE_IP=100.74.242.121

# Backend Configuration
# BACKEND_URL is set in dev.docker-compose.yml for container-to-container communication
# For local development without Docker, uncomment and modify:
# BACKEND_URL=http://localhost:60000
