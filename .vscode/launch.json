{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Mindscape",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go"
        }
    ]
}
